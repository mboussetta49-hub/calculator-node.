pipeline {
    agent any
    stages {
        stage('Installation') {
            steps { bat 'npm install' }
        }
        stage('Build') {
            steps { echo 'Application prête !' }
        }
    }
}
